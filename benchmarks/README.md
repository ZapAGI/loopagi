# Benchmarks

Performance benchmarks for LoopAGI components.

- Routing latency (keyword vs embedding vs LLM)
- Agent response time by model
- Tool throughput (commands per second)
- Memory search latency by collection size
- Voice STT/TTS latency
